# Normalize line endings: LF in the repo, native on checkout.
# Prevents the CRLF churn that made every doc edit look like a full rewrite.
* text=auto eol=lf

# Binary files: never touch.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
